# 이 파일이 바뀌면 Streamlit Cloud가 의존성을 다시 설치하면서 앱 프로세스를
# 통째로 재시작한다. 소스만 고쳐 푸시하면 파일 감시 기반 핫리로드가 일어나는데,
# 여러 모듈을 한꺼번에 고쳐 짧은 간격으로 연속 푸시하면 dashboard.py는 새 버전,
# calculate_metrics는 옛 버전이 sys.modules에 남는 어긋난 상태가 생길 수 있다
# (2026-08-05 실제 발생: 기준일/스코어가 옛 로직 결과로 표시되고 AttributeError).
# 그럴 때 이 파일을 건드려 푸시하면 깨끗한 재시작을 강제할 수 있다.
requests>=2.31.0
pandas>=2.0.0
streamlit>=1.32.0
